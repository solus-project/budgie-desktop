PKG_CHECK_MODULES(BUDGIE_DAEMON,
    [
        gtk+-3.0 >= gtk_required_version
        gio-unix-2.0 >= glib_required_version
    ]
)

AC_CONFIG_FILES([daemon/Makefile daemon/budgie-daemon.desktop.in])
